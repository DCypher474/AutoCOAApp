# Google Sheets via Apps Script (admin sets once)
# 1. Create a Google Sheet → Extensions → Apps Script → paste code from google-apps-script/Code.js → Deploy as Web app (Anyone).
# 2. Put the Web app URL here (ends with /exec):
VITE_SHEETS_SCRIPT_URL=https://script.google.com/macros/s/YOUR_SCRIPT_ID/exec
